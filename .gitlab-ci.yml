image: debian:unstable

stages:
    - build
    - test
    - deploy

build_job:
  stage: build
  script:
    - cmake .
    - make

test_job:
  stage: test
  script:
  - export DISPLAY=:1
  - make
  - ./ae-test
